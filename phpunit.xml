<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         failOnWarning="true"
         failOnRisky="true"
         beStrictAboutOutputDuringTests="true">

    <testsuites>
        <!--
            Unit Tests: framework structure, PHP syntax, and library logic.
            These run in CI without a live server or database.

            API tests (tests/Api/) require a running Apache + DB instance.
            Run them manually with: bash scripts/test_api.sh http://your-server
        -->
        <testsuite name="Unit Tests">
            <directory>tests</directory>
            <exclude>tests/Api</exclude>
            <exclude>tests/Libraries</exclude>
        </testsuite>
        <testsuite name="Library Tests">
            <directory>tests/Libraries</directory>
        </testsuite>
    </testsuites>

    <logging>
        <junit outputFile="tests/results/junit.xml"/>
    </logging>
</phpunit>
